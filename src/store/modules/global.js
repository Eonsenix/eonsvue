const state = {
}

const mutations = {
  RIGHT_DONE (state, headerRightClick) {
    state.headerRightClick = headerRightClick
  }
}

const actions = {

}

const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
